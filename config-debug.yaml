# MAVLink DroneBridge Configuration - DEBUG/TEST MODE
# ⚠️ This config allows running without a connected Pixhawk
# Use this for development/testing only!

# Logging settings
log:
  level: "debug"                         # Increased for debugging
  verbose: true                          # Enable verbose parsing

# Authentication settings
auth:
  enabled: true
  host: "45.117.171.237"
  port: 5770
  uuid: "0fd84717-c520-4d47-ba68-98e5dfcad160"
  shared_secret: "drone-fleet-shared-secret-auth-key-2025"
  keepalive_interval: 30
  session_heartbeat_frequency: 5

# Network settings
network:
  local_listen_port: 14542
  target_host: "45.117.171.237"
  target_port: 14550
  protocol: "udp"

# Ethernet settings - DEBUG MODE
ethernet:
  interface: ""
  local_ip: "10.41.10.1"
  broadcast_ip: ""
  pixhawk_ip: "10.41.10.2"
  auto_setup: true
  subnet: "24"
  allow_missing_pixhawk: true            # 🔴 DEBUG: Allow auth without Pixhawk
  pixhawk_connection_timeout: 5          # Short timeout for testing

# Web server settings
web:
  port: 8080
